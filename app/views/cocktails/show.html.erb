<h1><%= @cocktail.name %></h1>
​
  <ul>
  <% @cocktail.doses.each do |dose| %>
  <li><%= dose.description %>
      <%= dose.ingredient.name %>
  </li>
​
  <%= link_to "Delete #{dose.description}",
              dose_path(dose),
              method: :delete,
              data: { confirm: "You sure?" } %>
  <% end %>
​
  <br>  <br>
  <%= link_to "Create a new dose dans ton mix", new_cocktail_dose_path(@cocktail) %>
</ul>
​
​
  <p><%= link_to "back to list", cocktails_path %></p>
